# 更新日志

## 2026-09-05 - 3D 外部尺寸标注字号修复（高度/深度）

- 修复高度（竖排）与深度尺寸标注字号过小的问题：根因是标签像素预算打在"长度轴"而非"字厚轴"（竖排贴图旋转 90° 后 aspect<1，字高塌缩到 ~8px），且深度引导线沿 Z 轴被透视缩短后触发 22px 下限。现统一按字形厚度分配 65px 预算，fit 上限改用引导线正面视长（不再受视角透视缩短影响），下限提高到 24px。
- 修复"越放大越小"：放大视角时高度/深度标注随柜体一起变大（至 65px 字厚封顶），不再相对缩小；宽度/列宽标注默认视角下像素级不变。
- 删除 `LabelSprite` 逐帧投影引导线端点的计算（改为一次性取世界长度），消除极近景下投影翻转导致标注骤缩的隐患。
- 验证：`npx tsc --noEmit` 通过；无头 Chrome 实测默认/放大 2×/4×/缩小/双面岛台五种场景截图（`scripts/verify-dimension-labels.mjs`，截图在 `output/dimension-labels/`）。
- 详细说明见 [docs/2026-09-05-外部尺寸标注字号修复.md](docs/2026-09-05-外部尺寸标注字号修复.md)。

## 2026-09-05 - 设计器截图体验优化：隐藏选中框与点空白释放编辑

- 「导出图片」（顶栏）与「载图」（底部工具栏）在导出瞬间自动隐藏选中格子的黄色高亮框，导出完成后自动恢复编辑状态，导出 PNG 不再带黄框；C 端导出埋点（`exported` 里程碑）行为不变。
- 顶栏与底部工具栏新增「隐藏选中框」眼睛按钮（图标随状态切换），支持 `Esc` 快捷键切换；隐藏时悬浮「撤销/删除」工具条一并隐藏，点击任意格子自动恢复。
- 「结构」页签下点击 3D 视口空白处（背景或地面）释放编辑状态：取消选中、隐藏高亮框、左下角显示「未选中」、格子编辑区置灰，全局尺寸编辑保持可用；点击柜体立即恢复选中。框架/配件/颜色页签行为不变。
- 实现要点：`selection` 状态允许为空（`Selection | null`），`activeSelection` 推导不再把空值强制回退到最近格子；`selectCell(null)` 在结构页签下执行 `setSelection(null)`。
- 验证：`npx tsc --noEmit` 通过；Playwright 实测选中 → 点空白释放 → 点地面保持 → 点柜体恢复 → 导出无黄框全链路。
- 详细说明见 [docs/2026-09-05-选中框隐藏与释放编辑状态.md](docs/2026-09-05-选中框隐藏与释放编辑状态.md)。

## 2026-09-05 - 企业账号登录日志、移动端兼容修复与销售合同

- ERP 新增登录日志：API 侧记录每次账号成功登录的时间、IP 与设备信息（`/api/login-logs`，新增 `0017_login_logs` 迁移）；ERP 新增登录日志页面，平台超级管理员可按企业、时间范围和关键字检索。
- 修复 iPad（iPad Pro 13寸 2025，iOS Safari）无法删除钣金面板的问题：根因是原生 `window.confirm` 被 iOS 静默拦截（确认直接返回 false），替换为应用内自定义确认弹窗；同步完成移动端触控兼容审计与修复。
- 订单详情新增销售合同生成（`apps/erp/src/lib/sales-contract.ts`）：由订单数据渲染 HTML 合同，卖方信息可来自企业资料，金额按人民币格式化。
- 验证与细节见 [docs/移动端兼容审计与修复总结.md](docs/移动端兼容审计与修复总结.md)。

## 2026-09-05 - "全选新增一列"交互稳定性修复

- 修复克隆胶囊被渲染到屏幕外的问题：胶囊位置按当前相机视口钳制，贴边场景（如单列柜的左侧）也始终完整可见可点。
- 修复点击胶囊偶发变成"加一格"的问题：将"+"提示与克隆胶囊统一为屏幕空间就近命中判定，点胶囊附近必触发整列克隆，点"+"附近才是单格新增，不再依赖 3D 射线是否恰好命中胶囊。
- 克隆胶囊失焦宽限期从 1.2 秒延长至 2 秒，鼠标从"+"移向胶囊途中经过空白区域不会中途消失。
- 整列克隆行为确认：门板、配件、颜色等全套配置随列复制，源列中的空洞同样保留。
- 验证：`pnpm typecheck` 通过；`scripts/verify-single-cell-clone.ts` 19 项断言全部通过；浏览器实测左/右向克隆（含顶格下翻门场景）均正确。

## 2026-09-04 - 3D 结构扩展交互改造：单击加一格 + 全选新增一列

- 3D 结构页签中点击"+"扩展方向按钮改为每次只新增一格：网格边缘扩展时新行/列仅激活紧邻选中格的那一格，其余位置为不可见占位（`expandCell`/`insertColumn`/`insertRow`/`insertDepthSegment` 支持激活位参数），不再一次生成整排/整列。
- 新增"全选新增一列"交互：光标悬停选中格顶部、左侧或右侧的"+"号时，对应方向外侧浮现胶囊按钮，点击后将当前格所在整列（所有层×深度，含门板、配件、颜色等全套配置）克隆为相邻新列（`cloneColumn`，右侧/顶部悬停新列在右、左侧悬停新列在左），并整列黄色高亮展示；点击任意格子或撤销时退出整列选中。
- 外部尺寸标注按启用格自动重算，源列中的空洞（禁用格）同样按同配置克隆；列数达到上限 10 列时按钮自动隐藏；C 端注册门槛与撤销历史沿用现有 `updateConfig` 通道。
- 验证：`pnpm typecheck` 通过；`pnpm test` 89/89 通过；新增 `scripts/verify-single-cell-clone.ts` 覆盖单格扩展、整列克隆、空洞克隆与上限守卫。

## 2026-08-28 - 宝塔环境清理与发布流程固化

- 清理宝塔 `/www/wwwroot/jimuces1` 中旧/新两套 Compose 实例的全部容器、PostgreSQL/MinIO 数据卷和项目网络，保留源码目录用于上传新版。
- 完成 HTTPS 首页、ERP、API 健康检查和前端资源校验，确认不再引用服务器 IP。
- 新增 [宝塔 Docker 发布与回滚流程](docs/BAOTA_RELEASE_RUNBOOK.md)，记录发布前检查、Git 同步、固定 Compose 重建、端口冲突处理、C 端验收、回滚和彻底清空流程。
- 明确日常更新不使用 `docker compose down -v`；只有明确清空实例时才按容器名和卷名执行破坏性清理。

## v4.22.0-cportal.1 - 2026-08-24

### 新增

- C 端首页公开展示真实 3D 基础一格，未注册用户可直接查看基础预览。
- 新增注册悬浮窗：新增结构、模块、配件、颜色或点击其他受限模块时，引导用户注册或登录。
- 注册/登录后解锁模块化配置操作，并隐藏 C 端不应展示的 ERP 价格、BOM 与内部配件清单。
- ERP 新增“C端使用端口”配置，可启用/停用门户、设置门户 slug、配置注册后开放模块和企业客服验证码。
- 新增客户模型事件记录与 ERP 时间线查看，覆盖 `opened`、`first_generated`、`config_changed`、`saved`、`exported`、`consultation_submitted`。
- Vite 开发环境新增 `/api` 到 ERP API 的代理配置。

### 验证

- 通过 `pnpm typecheck`。
- 通过 `pnpm build:designer`、`pnpm build:api` 与 `pnpm build:erp`。
- 通过 `pnpm test`，API 测试 88/88 全部通过。
- 通过 `git diff --check`。

### 已知限制

- 当前门户配置、客户账号和模型事件使用进程内存存储，API 重启后会清空；后续接入持久化存储时需保留现有事件字段和接口语义。

## 4.22.0 - 2026-08-21

### 新增

- 新增上翻门与下翻门的 3D 参考模型：基于闭合、开启两套 FBX 状态生成可在场景中插值开合的 GLB 资源。
- 新增门体参考模型构建脚本，自动区分门板、锁具、随动铰链与固定五金。
- 新增上翻门硬件验证脚本，覆盖闭合、半开和全开三种状态。

### 改进

- 上翻门与下翻门支持点击切换开合和拖拽调整开度，门体动画保持平滑过渡。
- 参考模型加载失败时，场景自动回退到原有的程序化门体与五金效果。
- 将参考模型的门板、锁具及铰链按真实开合状态同步变换，改善门体交互的视觉一致性。

### 验证

- 完成上翻门、下翻门的点击开合和画面变化验证。
- 通过 TypeScript 类型检查与生产构建。
